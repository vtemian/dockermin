# Copy to .env (gitignored) and fill in. Do not commit .env.

# Prime Intellect API key for compute provisioning
PRIME_INTELLECT_API_KEY=

# Hugging Face token (write scope for pushing dataset + adapter)
HF_TOKEN=

# Weights & Biases for training run telemetry
WANDB_API_KEY=
WANDB_PROJECT=dockermin
WANDB_ENTITY=vladtemian

# Anthropic API key for synthetic dataset variants + Claude Code agent-loop baseline
ANTHROPIC_API_KEY=

# OpenAI API key for GPT-4o baseline eval
OPENAI_API_KEY=
